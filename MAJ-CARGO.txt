===================================================================
MAJ-CARGO.txt — Container Optimizer — Déploiement 29 Mai 2026
===================================================================

URL Vercel (Production):
  https://cargo-8iklp7zdd-parisb2bs-projects.vercel.app

Domaine (alias Vercel):
  https://cargo.sasfr.com (⚠ DNS IONOS à configurer)

Firebase:
  Project ID : cargo-optimizer-fca82
  Auth       : Google Sign-In (configuré dans index.html)
  Firestore  : Collection cargo_sessions/{uid}_{id}
  Règles     : firestore.rules (⚠ à déployer manuellement via Console Firebase)

GitHub:
  Repo    : parisb2b/cargo (PUBLIC)
  Branche : main

===================================================================
ÉTAT DU DÉPLOIEMENT
===================================================================

✅ Étape 1 — Git + GitHub
   - Repo: parisb2b/cargo, branche: main
   - vercel.json configuré (@vercel/static)
   - Push automatique après chaque étape

✅ Étape 2 — Firebase Auth + Firestore dans index.html
   - SDK Firebase (module) : Auth + Firestore
   - Auth section dans la nav (.nav-right)
   - Auth-wall overlay (écran de connexion bleu)
   - saveAll() avec sync Firestore (ownerId)
   - loadAll() avec fallback localStorage
   - Fonctions d'auth: doSignIn(), doSignOut(), getUserId()
   - startFirestoreSync() avec onSnapshot temps réel
   - deleteSession() avec deleteDoc Firestore
   - INIT avec firebase-ready event + timeout 3s fallback
   - 🔧 Bug corrigé: backtick échappé (\`) dans renderColisTable()
     (bug préexistant dans container-optimizer-final.html)

⚠ Étape 3 — Règles Firestore
   - firestore.rules créé dans le repo
   - ⚠ À DÉPLOYER MANUELLEMENT : Firebase Console > Firestore > Règles
     (le compte CLI n'a pas les permissions sur cargo-optimizer-fca82)

✅ Étape 4 — Déploiement Vercel
   - Production: https://cargo-8iklp7zdd-parisb2bs-projects.vercel.app
   - SSO Protection désactivée (accès direct)
   - Alias: cargo.sasfr.com

⚠ Étape 5 — Domaine cargo.sasfr.com
   - Domaine ajouté dans Vercel
   - ⚠ DNS IONOS : A cargo.sasfr.com → 76.76.21.21
   - ⚠ Firebase Auth : Ajouter cargo.sasfr.com aux domaines autorisés

✅ Étape 6 — Vérifications (12 points)
   1. ✅ URL accessible → auth-wall overlay bleu
   2. ⚠ Connexion Google → domaine Firebase Auth à configurer
   3. ✅ Créer chargement → modal fonctionnel
   4. ✅ Sélection conteneur (20/40 GP, 40 HC, Personnalisé)
   5. ✅ Table colis + ⧉ dupliquer + × supprimer
   6. ✅ Stats m³, taux remplissage, kg, colis
   7. ✅ Navigation onglets (1-4)
   8. ✅ Sauvegarde automatique (badge Sauvegardé)
   9. ✅ Toggle FR/中文 — traduction complète
   10. ✅ Boutons Import/Export Excel
   11. ✅ 0 erreur JavaScript console
   12. ✅ Bug backtick échappé corrigé

===================================================================
ACTIONS RESTANTES
===================================================================

1. [ ] Déployer firestore.rules (Console Firebase > Firestore)
2. [ ] DNS IONOS: A cargo.sasfr.com → 76.76.21.21
3. [ ] Firebase Auth: Ajouter cargo.sasfr.com aux domaines autorisés
4. [ ] Tester connexion Google avec domaine cargo.sasfr.com
5. [ ] Vérifier sync multi-navigateurs

===================================================================
DERNIER COMMIT
===================================================================

feat: full Firebase Auth + Firestore integration with authenticated sync
2249494 — 29 Mai 2026
